# FAF Agent Card (.fafa) — mcp-trinity reference server
# Media type: application/vnd.fafa+yaml
# This is the reference server's own identity card. A fork replaces it
# with its own agent's real identity, capabilities, and live endpoints.
# Every scalar quoted, matching the live convention on faf.one/.well-known/fafa.

version: "1.0"
agent:
  name: "mcp-trinity"
  displayName: "mcp-trinity"
  vendor: "reference"
  version: "0.2.0"
  description: >-
    Reference MCP server demonstrating project context, persistent memory,
    and agent identity through the Server Card _meta block and a
    self-published ai-catalog.json. A fork fills this in with its own.
  status: "reference"
  license: "MIT"
